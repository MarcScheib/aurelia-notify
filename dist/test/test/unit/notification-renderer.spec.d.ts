import { BSNotification } from '../../src/bs-notification';
export declare let defaultSettings: {
    append: boolean;
    containerSelector: string;
    timeout: number;
    viewModel: typeof BSNotification;
    limit: number;
};
